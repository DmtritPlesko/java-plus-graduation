spring:
  jpa:
    show-sql: true
    properties:
      hibernate:
        jdbc:
          time_zone: UTC
  datasource:
    driverClassName: org.postgresql.Driver
    url: jdbc:postgresql://localhost:6666/main-server
    username: user
    password: 12345
  sql:
    init:
      mode: always